#include <iostream>
#include <vector>

#ifndef NI_EVY_PRINTFUNCTIONS_H
#define NI_EVY_PRINTFUNCTIONS_H

#endif //NI_EVY_PRINTFUNCTIONS_H

template<typename T, typename C>
void printVector(const std::vector<T> &);

template<typename T, typename C>
void printVector(const std::vector<T> &, char);

void printStringVector(const std::vector<int> &, char);

